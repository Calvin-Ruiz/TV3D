>>> OpenGL 4.4 version <<<
Escape -> pause
Space, Return -> unpause
Q, close window, termination signal -> Exit
set "FAST off" in config.txt to scale sampler2DRect to window size

compile.sh may need to be run twice the first time
